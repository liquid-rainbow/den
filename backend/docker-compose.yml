version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: den_postgres
    restart: always
    environment:
      POSTGRES_DB: den_db
      POSTGRES_USER: den_user
      POSTGRES_PASSWORD: den_password
    ports:
      - "5434:5432"
    volumes:
      - den_pgdata:/var/lib/postgresql/data

volumes:
  den_pgdata:
