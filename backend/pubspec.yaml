name: den_backend
description: DEN Server Application built in Dart using Shelf and PostgreSQL.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^3.4.0
  crypto: ^3.0.3
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.2
  http: ^1.2.0
